---
title: "➡️ Subscribe to Updates"
description: "Recieve an email whenever we have new release notes"
---
<div id="hubspot-form"></div>

{(() => {
  // Wait until the DOM is fully loaded
  if (typeof window !== "undefined") {
    (function() {
      var script = document.createElement('script');
      script.src = '//js.hsforms.net/forms/embed/v2.js';
      script.charset = 'utf-8';
      script.type = 'text/javascript';

      // Ensure form creation only happens after the script has been loaded
      script.onload = function() {
        if (window.hbspt) {
          hbspt.forms.create({
            region: "na1",
            portalId: "24337717",
            formId: "09191e60-1acb-4122-835f-bddf5b97f326",
            target: '#hubspot-form' // Ensure it targets the correct div
          });
        }
      };

      document.body.appendChild(script); // Append the script to load it
    })();
  }
})()}