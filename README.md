# Docs
This repo powers [Onboarded documentation site](https://docs.onboarded.com).

# Development
1. Make edits locally.
2. Preview them by running the docs locally via `pnpm run:server`.
3. Commit your changes and either open a PR or push up `main`.
4. New commits on `main` will be automatically deployed to Mintlify.
